node:                         # Config is hierarchical
  loompa: 10                  # Simple value
  zippity: ${node.loompa}     # Value interpolation
  do: "oompa ${node.zippity}"  # String interpolation
  waldo: ???                  # Missing value, must be populated prior to access
